### 🚀 Stock Plugin v2.0.10 更新日志

- •  【修复】 下架 arm64ec 预编译包：此前发布的 arm64ec 包实际是 2025-03-08 的 Stock v1.13 旧版本（与上游 Stock_V1.13_arm64ec.zip 字节完全一致），发包脚本只对它改名、从未重新编译，却被贴上此后每一个新版本号，下载该包会缺失全部新功能；现不再发布该架构预编译包，发包脚本改为只打 x64/x86 并在打包前校验产物版本与去除残留包；ARM 设备请用 x64 版（ARM64EC 版 TrafficMonitor 可正常加载 x64 插件），确需原生版可按 README「编译 ARM64EC」一节自行编译
- •  【新增】 资金流向页底部的「机构领头」「主力领头」股票名称现可点击，直接跳转该股日K线临时查看（右键返回行情中心）；悬停时股名带下划线并变手型光标
- •  【修复】 从行情中心点击领头股/主力股名跳到日K后，关掉重开悬浮窗再右键，行情中心会退回默认的「板块资金流」页而回不到刚才那一页（页签与跳转来源股票只在「隐藏暂存」时保留，从任务栏重开的换绑重建会丢）；现在两种重建方式都会保留浏览页签与临时视图来源股票，右键能回到跳转前那一页
- •  【新增】 涨跌趋势页的涨跌分布柱现可点击，弹出该档成分股列表（名称/代码/现价/涨跌幅），点任意一行跳转该股日K线（右键返回行情中心）；涨停与跌停两档直接用已抓到的涨停/跌停池名单、点开即出，其余档按涨跌幅区间向服务端分页取名单、滚到接近末尾自动续取；浮层只绘制可见行，五千余只也不卡
- •  【修复】 从行情中心点股票跳去日K后，右键返回要干等好几秒才回到原页面：分时缓存原来是逐点写库（一天 240 个点就是 240 次提交），中途把全局数据库锁攥住八九秒，而切换股票、从K线返回行情中心都要同步读一次缓存，只能干等这把锁；现改为整批一个事务写入（同类问题的基金净值缓存、分时成交明细一并修正），「删旧+写新」也随之变成原子操作，不会再有半截数据
- •  【优化】 行情中心主力资金页的第三条曲线由「ETF净流入」改为「创业板净流入」：ETF 没有全市场聚合的分时资金流接口，那条线只能靠插件开着时本地逐分钟攒点，重启、换日都会清零，因此长期画不出来；创业板指是服务端直接下发整日 240 个点，与沪、深两条线同级可靠，重启即恢复。原「资金净申购」页的 ETF 排行不受影响
- •  【修复】 修正 09-18 那次 ETF 曲线修复的遗留缺陷：采样只在 ETF 排行页被拉取时才触发，主力资金页从不请求该数据，导致曲线即便服务端可用也攒不到点；改用服务端数据后此依赖一并去除

---
### 📦 安装包说明
- **x64 推荐版**：适用于绝大多数 64 位 Windows 系统及 64 位 TrafficMonitor
- **x86 兼容版**：适用于 32 位系统环境

> **ARM64 设备（骁龙 / Surface Pro X 等）**：请直接使用上面的 **x64 版**——ARM64EC 版 TrafficMonitor
> 可以正常加载 x64 插件（走系统模拟执行，功能完全一致，仅速度非原生）。
> 本项目不再提供 ARM64EC 预编译包；确需原生版本请参照 README 的「编译 ARM64EC」一节自行编译。
